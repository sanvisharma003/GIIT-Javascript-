- for loops are really optimised and fast in javascript.
var arr = ["hj", "hf", "ir"]

incrementing the variable
for (var i = 0; i < arr.length; i++) 
{
  console.log(arr[i]);
}

deincrementing the variable
for (var i = arr.length-1; i >= 0; i--) 
{
  console.log(arr[i]);
}

for(var index in arr)
{
  console.log( arr[index] );
}
